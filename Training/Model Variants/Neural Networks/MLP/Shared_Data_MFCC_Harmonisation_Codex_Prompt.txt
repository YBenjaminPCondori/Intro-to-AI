CODEX CONTINUATION PROMPT — HARMONISE SHARED DATA + MFCC FOUNDATION
================================================================

PROJECT
-------
Continue refactoring the private GitHub repository:

YBenjaminPCondori/Intro-to-AI

This is a SAFETY / HARMONISATION PASS that must be completed BEFORE applying the final staged MLP, CNN and LSTM hyperparameter-screening/HPO refactors.

The purpose is to remove conflicting ownership of:

- dataset scanning;
- metadata parsing;
- train/validation/test splitting;
- MFCC extraction;
- feature caching;
- class weights;

while PRESERVING the already-implemented educational MFCC walkthrough.

Do NOT redesign the modelling task.
Do NOT run expensive model training.
Do NOT fabricate results.

The final design must be:

ONE canonical dataset audit + ONE canonical frozen split
                         ↓
ONE shared raw MFCC matrix cache
                         ↓
          ┌──────────────┼──────────────┐
          ↓              ↓              ↓
         MLP            CNN            LSTM
          ↓              ↓              ↓
   mean/std 80-D      MFCC map      MFCC sequence
      features        40 × T × 1        T × 40
          ↓              ↓              ↓
 one MLP-ready     one CNN-ready     one LSTM-ready
    cache              cache              cache

No experiment notebook may independently rescan, resplit, or re-extract MFCCs.


================================================================
1. IMPORTANT CURRENT-REPOSITORY CONTEXT
================================================================

The existing analysis notebook is:

Training/Model Variants/Analysis/MLAAD_Scan_MFCC_Analysis_cache.ipynb

It currently contains an educational MFCC visual walkthrough that has ALREADY BEEN IMPLEMENTED.

PRESERVE that walkthrough.

Do not delete it.
Do not substantially rewrite its plots.
Do not move it into each model notebook.
Do not create separate copies for MLP, CNN and LSTM.

The existing analysis notebook currently contains wording stating that it does NOT create a shared feature dependency and that individual model notebooks perform their own preprocessing/MFCC extraction.

That statement is now outdated and conflicts with the desired final architecture.

Update that wording.

The analysis notebook will become the authoritative shared DATA / MFCC FOUNDATION notebook.

The MLP, CNN and LSTM model folders will consume its saved artifacts.


================================================================
2. DO NOT DAMAGE THE EDUCATIONAL MFCC WALKTHROUGH
================================================================

Preserve the existing section:

## MFCC Step-by-Step Visual Explanation

and its educational flow approximately:

Raw WAV
→ fixed-length waveform
→ windowed frame
→ FFT / frequency spectrum
→ Mel triangular filterbank
→ Mel spectrogram
→ log-Mel spectrogram
→ MFCC matrix
→ MFCC mean/std aggregation

Keep:
- the existing explanatory/noisy educational comment style;
- one example audio item;
- robust error handling;
- plots saved to FIGURES_DIR;
- existing audio/MFCC constants where possible.

IMPORTANT CLARIFICATION:

Step 10, MFCC mean/std aggregation, describes the MLP/SVM-style fixed-vector representation.

It is NOT the representation used directly by the CNN or LSTM.

After the existing walkthrough, add a short Markdown section:

## How the Same MFCC Matrix Feeds Different Models

Explain visually/textually:

                         MFCC matrix
                            40 × T
                              |
              ┌───────────────┼───────────────┐
              ↓               ↓               ↓
             MLP             CNN             LSTM
              |               |               |
       mean + std       retain 2D map    transpose/retain
              |            + channel       time sequence
              |               |               |
            80-D          40 × T × 1        T × 40
           vector             map           sequence

Explain:

- MLP needs a fixed-length vector, therefore 40 coefficient means + 40 coefficient standard deviations = 80 features.
- CNN preserves the coefficient-by-time map so convolutional filters can learn local patterns.
- LSTM preserves temporal ordering so recurrent units can process the MFCC sequence over time.

Do NOT imply that CNN/LSTM consume the 80-dimensional mean/std vector.


================================================================
3. CORRECT METADATA ONCE, CENTRALLY
================================================================

The current/old scanner has used logic equivalent to:

language = path.parent.name

This is wrong for synthetic MLAAD paths such as:

fake/en/Llasa-1B-Multilingual/example.wav

For synthetic MLAAD data structured approximately:

fake/<language>/<tts_generator>/<audio_file>

store:

language = <language>
tts_generator = <tts_generator>

For bona-fide data:
- derive language only from reliable path/metadata structure;
- set tts_generator = "bona_fide".

The canonical manifest should contain at least:

path
relative_path
label
class_name
language
tts_generator
file_size_mb

Labels remain:

0 = bona_fide
1 = synthetic

If a genuine speaker/source/original-utterance identifier can be reliably derived, preserve it.

Do NOT invent grouping metadata.


================================================================
4. DATASET AUDIT REMAINS CENTRAL
================================================================

The analysis notebook remains responsible for the dataset audit.

Preserve or add:

- total recordings;
- bona-fide count;
- synthetic count;
- class ratio;
- language distribution;
- class × language distribution;
- synthetic recordings by TTS generator;
- duplicate paths;
- missing files;
- audio-duration distribution;
- native sample-rate distribution by class;
- reliable group/source distribution if genuinely available.

Do not duplicate this full dataset audit in MLP/CNN/LSTM notebooks.


================================================================
5. ONE CANONICAL FROZEN SPLIT
================================================================

There must be ONE authoritative split for ALL models.

Target:

train = 70%
validation = 15%
test = 15%

Base seed:

RANDOM_STATE = 42

If a reliable group identifier exists:
- use a group-aware split;
- guarantee zero group overlap.

If no reliable group identifier exists:
- do not invent one;
- use reproducible label-stratified splitting;
- print/document the limitation.

IMPORTANT OLD-TEST RULE:

Previous pilot workflows have already exposed test results.

Therefore:

- inspect whether the repository already contains a NEW replacement split created specifically after that issue was identified;
- if a valid untouched replacement split already exists, reuse it;
- otherwise archive/retire the old split and create ONE new final canonical split.

Do NOT repeatedly regenerate a new split on every notebook execution.

Once the canonical split exists, save and reuse it.

Save under one clear shared location, preferably:

outputs/shared/manifests/
    train_manifest.csv
    validation_manifest.csv
    test_manifest.csv
    split_config.json

or preserve an existing equivalent shared path if already established.

split_config.json should record at least:

random_state
train_fraction
validation_fraction
test_fraction
group_key_used_or_null
creation_method
audio/MFCC configuration identity where useful

Validate:

- split sizes;
- class counts;
- class ratios;
- zero path overlap;
- zero group overlap when grouping exists.

MLP, CNN and LSTM must all use these exact rows.

No model notebook may call train_test_split() to create its own modelling partitions.


================================================================
6. ONE SHARED RAW MFCC MATRIX CACHE
================================================================

MLP, CNN and LSTM currently use the same basic MFCC extraction family.

Avoid extracting the same MFCCs independently three times.

The shared analysis/data-foundation notebook should create ONE raw MFCC matrix cache.

Use the existing common settings unless a documented correction already exists:

SAMPLE_RATE = 22050
FIXED_DURATION_SECONDS = 5.0
N_MFCC = 40
N_FFT = 1024
HOP_LENGTH = approximately 10 ms using the existing calculation
WIN_LENGTH = approximately 25 ms using the existing calculation

Derive EXPECTED_FRAMES programmatically.

Do NOT hard-code 497 if it can be derived from the active constants.

For each recording, create a consistent raw MFCC matrix:

shape approximately:

(40, EXPECTED_FRAMES)

Use one consistent audio loading / resampling / padding / truncation implementation.

Use float32 for cached feature arrays unless the current project has a documented reason not to.

Save ONE shared cache, preferably:

outputs/shared/mfcc_cache/
    X_train_mfcc.npy
    y_train.npy
    train_metadata.csv

    X_validation_mfcc.npy
    y_validation.npy
    validation_metadata.csv

    X_test_mfcc.npy
    y_test.npy
    test_metadata.csv

    mfcc_config.json

The leading sample order MUST match the corresponding canonical manifest order exactly.

Add checks proving:

len(X_train_mfcc) == len(train_manifest)
len(X_validation_mfcc) == len(validation_manifest)
len(X_test_mfcc) == len(test_manifest)

and labels align row-for-row.

Do NOT create separate raw MFCC extraction caches for:

MLP
CNN
LSTM
baseline
activation trial
optimizer trial
dropout trial
architecture trial
Random Search
GA

The raw MFCC matrix is the common source representation.


================================================================
7. SHARED CLASS WEIGHTS
================================================================

Compute class weights ONCE from the canonical y_train only.

Save:

outputs/shared/class_weights.json

or an existing equivalent shared location.

All MLP/CNN/LSTM experiments must load the same class weights.

Do NOT calculate different class weights from independently created model splits.


================================================================
8. MODEL-SPECIFIC REPRESENTATIONS
================================================================

The models share the SAME recordings and SAME raw MFCC matrices but use different shaped/derived representations.

It is acceptable to create ONE model-ready cache per model.

It is NOT acceptable to create a new cache per experiment notebook.


----------------------------------------------------------------
8.1 MLP
----------------------------------------------------------------

MLP data preparation must LOAD:

X_train_mfcc
X_validation_mfcc
X_test_mfcc

from the shared raw MFCC cache.

Do NOT rescan audio.
Do NOT split data.
Do NOT call librosa MFCC extraction again.

Derive:

mfcc_mean = mean over time for each of 40 coefficients
mfcc_std = standard deviation over time for each of 40 coefficients

Concatenate:

40 means + 40 standard deviations = 80-dimensional vector.

Expected model-ready shapes:

X_train_mlp:
(n_train, 80)

X_validation_mlp:
(n_validation, 80)

X_test_mlp:
(n_test, 80)

Fit StandardScaler on X_train_mlp ONLY.

Apply that fitted scaler to validation/test.

Save one MLP-ready cache, for example:

outputs/mlp/cache/
    X_train.npy
    X_validation.npy
    X_test.npy
    y_train.npy
    y_validation.npy
    y_test.npy
    scaler.joblib
    feature_config.json

Downstream MLP Baseline / screening / Random Search / GA / comparison / final notebooks LOAD this model-ready cache.

They must not independently derive/cache MFCCs.


----------------------------------------------------------------
8.2 CNN
----------------------------------------------------------------

CNN data preparation must LOAD the SAME shared raw MFCC matrices.

Do NOT rescan audio.
Do NOT create a CNN-specific split.
Do NOT call Librosa MFCC extraction again.

Transform:

(n_samples, 40, T)

to:

(n_samples, 40, T, 1)

for Conv2D input.

Preserve the existing train-only MFCC-map normalisation principle.

Estimate normalisation statistics from TRAINING data only.

Apply the same training-derived statistics to validation/test.

Save one CNN-ready cache, for example:

outputs/cnn/cache/
    X_train.npy
    X_validation.npy
    X_test.npy
    y_train.npy
    y_validation.npy
    y_test.npy
    mfcc_mean.npy
    mfcc_std.npy
    feature_config.json

Downstream CNN Baseline / screening / Random Search / GA / comparison / final notebooks LOAD this cache.

They must not independently derive/cache MFCCs.


----------------------------------------------------------------
8.3 LSTM
----------------------------------------------------------------

LSTM data preparation must LOAD the SAME shared raw MFCC matrices.

Do NOT rescan audio.
Do NOT create an LSTM-specific split.
Do NOT call Librosa MFCC extraction again.

Transform:

(n_samples, 40, T)

to:

(n_samples, T, 40)

so the LSTM receives:

time steps × MFCC features.

Preserve temporal order.

Do NOT mean/std aggregate the MFCC matrix for LSTM.

Estimate sequence-normalisation statistics using TRAINING data only, consistent with the existing LSTM methodology.

Apply the same training-derived statistics to validation/test.

Save one LSTM-ready cache, for example:

outputs/lstm_optional/cache/
    X_train.npy
    X_validation.npy
    X_test.npy
    y_train.npy
    y_validation.npy
    y_test.npy
    mfcc_mean.npy
    mfcc_std.npy
    feature_config.json

Downstream LSTM Baseline / screening / Random Search / GA / comparison / final notebooks LOAD this cache.

They must not independently derive/cache MFCCs.


================================================================
9. IMPORTANT DISTINCTION: SHARED CACHE VS MODEL-READY CACHE
================================================================

Use this architecture:

A. ONE shared RAW MFCC cache:
   outputs/shared/mfcc_cache/

B. AT MOST ONE derived/model-ready cache per model:
   outputs/mlp/cache/
   outputs/cnn/cache/
   outputs/lstm_optional/cache/

This is NOT redundant MFCC extraction.

The expensive Librosa MFCC operation happens once.

The model-specific caches only reshape, aggregate, or normalise the shared matrices.

Do not create model-ready caches for every individual trial notebook.


================================================================
10. UPDATE THE THREE MODEL DATA-PREPARATION STAGES
================================================================

The final staged model workflows should each begin with:

00_MLP_Data_Preparation.ipynb
00_CNN_Data_Preparation.ipynb
00_LSTM_Data_Preparation.ipynb

These are MODEL REPRESENTATION preparation notebooks, NOT dataset splitting notebooks.

Their responsibility is:

MLP 00:
shared MFCC matrices
→ 80-D mean/std vectors
→ train-only StandardScaler
→ one MLP-ready cache

CNN 00:
shared MFCC matrices
→ add one channel
→ train-only map standardisation
→ one CNN-ready cache

LSTM 00:
shared MFCC matrices
→ transpose to time × coefficients
→ train-only sequence standardisation
→ one LSTM-ready cache

None of these notebooks owns:
- dataset scanning;
- metadata parsing;
- canonical split creation;
- expensive Librosa MFCC extraction.


================================================================
11. DOWNSTREAM NOTEBOOK RULE
================================================================

After the relevant model 00 notebook has run:

all later model notebooks must load prepared arrays.

They must NOT contain active code that:

- scans the full audio dataset;
- generates train/validation/test splits;
- extracts MFCCs from WAV files;
- independently fits a new preprocessing transform;
- creates a separate cache for that experiment.

Keep methodology visible, but load the prepared artifacts.

It is acceptable to include short Markdown explaining how those artifacts were produced.


================================================================
12. TEST ISOLATION
================================================================

The shared analysis notebook and model-specific 00 notebooks may PREPROCESS and CACHE X_test/y_test.

That is not model evaluation.

However:

- no model predictions may be made on test data during preparation;
- no test accuracy/F1/precision/recall/loss may be calculated;
- test performance must not influence feature, architecture, or hyperparameter choices.

For each MLP/CNN/LSTM staged workflow:

Baseline
controlled screening
Random Search
GA
HPO comparison
multi-seed confirmation

must use TRAIN + VALIDATION only.

Only the final selected-model notebook may compute test performance.


================================================================
13. PRESERVE THE STAGED HPO DESIGN
================================================================

This harmonisation pass must NOT undo the already-planned staged workflow.

The intended later sequence for each applicable model remains conceptually:

Data/representation preparation
→ Baseline
→ controlled hyperparameter sensitivity screening
→ controlled-trial comparison
→ evidence-informed search space
→ Random Search
→ Genetic Algorithm
→ equal-budget HPO comparison
→ confirmation seeds [42, 123, 2026]
→ final selected model
→ held-out test once

Do NOT implement a broad shortcut that returns to:

Baseline → Random Search immediately

for MLP/CNN if their final staged master prompts specify screening first.

For LSTM, preserve its current status as optional unless the repository/report has explicitly promoted it to a core model.

Do not silently change the research scope.


================================================================
14. KEEP MODEL REPRESENTATIONS SCIENTIFICALLY DISTINCT
================================================================

Do not accidentally force every model to consume the same final-shaped feature array.

They share the SOURCE MFCC information, not necessarily the final shape.

Required distinction:

MLP:
(40, T)
→ time aggregation
→ 80-D fixed vector

CNN:
(40, T)
→ retain map
→ (40, T, 1)

LSTM:
(40, T)
→ transpose
→ (T, 40)

This distinction should be visible to an examiner.


================================================================
15. README / DOCUMENTATION UPDATE
================================================================

Update relevant README/documentation so it no longer states that each model independently scans/splits/extracts MFCCs.

Document the new dependency order clearly:

1. Run:
   Training/Model Variants/Analysis/MLAAD_Scan_MFCC_Analysis_cache.ipynb

   This:
   - audits dataset;
   - fixes/records metadata;
   - creates/loads canonical frozen split;
   - preserves educational MFCC walkthrough;
   - creates shared raw MFCC matrix cache;
   - saves shared class weights.

2. Run model-specific data preparation:

   00_MLP_Data_Preparation.ipynb
   00_CNN_Data_Preparation.ipynb
   00_LSTM_Data_Preparation.ipynb (only if using optional LSTM)

3. Run the remaining staged notebooks for the desired model.

Explain that all models therefore operate on the SAME train/validation/test observations.


================================================================
16. NO CUSTOM HELPER MODULE
================================================================

Do not create a required project-specific runtime helper module such as:

mlaad_colab_common.py
feature_common.py
data_helpers.py
cnn_common.py
lstm_common.py

Important logic should remain inspectable in notebooks.

Shared saved DATA artifacts are explicitly allowed and required.

Do not confuse a saved .npy/.csv/.json/.joblib artifact with hidden code abstraction.


================================================================
17. DO NOT RUN EXPENSIVE WORK AUTOMATICALLY
================================================================

Do not automatically perform:

- full shared MFCC extraction over the entire dataset;
- full model training;
- Random Search;
- GA;
- confirmation runs;
- final test evaluation.

Refactor the notebooks and make them ready to run.

Perform only lightweight/static validation where possible.

If a tiny synthetic/limited smoke test is needed to validate shapes or code paths, make it clearly optional and ensure it does NOT overwrite final canonical artifacts.


================================================================
18. STATIC CROSS-CHECKS
================================================================

Before finishing, verify statically:

1. Exactly one canonical manifest source is referenced by MLP/CNN/LSTM.
2. No model notebook creates an independent split.
3. Corrected language and TTS-generator columns are preserved.
4. The educational MFCC walkthrough still exists.
5. The walkthrough's plots have not been unnecessarily duplicated into model notebooks.
6. Step 10 mean/std is clearly identified as the MLP/fixed-vector branch.
7. One shared raw MFCC cache path exists.
8. Raw MFCC extraction is not duplicated across MLP/CNN/LSTM.
9. Shared raw feature matrices preserve manifest row order.
10. Shared labels align with manifests.
11. MLP representation is 80-D mean/std.
12. CNN representation retains 40 × T × 1.
13. LSTM representation retains T × 40.
14. MLP scaler is fitted on training only.
15. CNN normalisation is fitted on training only.
16. LSTM normalisation is fitted on training only.
17. Shared class weights come from canonical y_train only.
18. No preparation/trial/HPO notebook computes test metrics.
19. Existing pilot/legacy results are not mixed with the new final split.
20. No results are fabricated.
21. No active downstream notebook requires a custom helper module.
22. README execution order is updated and internally consistent.


================================================================
19. PROTECT EXISTING WORK
================================================================

Do not delete useful educational or experimental code merely because the architecture changes.

Use Git history / careful migration.

Specifically protect:

- the implemented MFCC educational walkthrough;
- useful dataset-audit plots/tables;
- existing TensorFlow/Keras model definitions where still methodologically correct;
- existing output conventions where compatible.

Only remove or disable code that would conflict with the new single-source data foundation, such as duplicate active split generation or duplicate MFCC extraction in downstream notebooks.


================================================================
20. FINAL OUTPUT FROM CODEX
================================================================

At completion, return a concise report containing:

A. Analysis notebook changes
- what was preserved;
- what became authoritative;
- metadata corrections;
- canonical split path;
- shared MFCC cache path;
- class-weight path.

B. MLP preparation changes
- shared inputs loaded;
- 80-D representation;
- scaler;
- cache paths.

C. CNN preparation changes
- shared inputs loaded;
- MFCC-map representation;
- train-only normalisation;
- cache paths.

D. LSTM preparation changes
- shared inputs loaded;
- MFCC-sequence representation;
- train-only normalisation;
- cache paths.

E. Removed/conflicting logic
- independent scanning removed/disabled;
- independent splitting removed/disabled;
- repeated Librosa MFCC extraction removed/disabled.

F. Static validation
- manifest dependencies;
- cache dependencies;
- shapes;
- test-isolation checks.

G. Exact execution order from this point forward.

Do NOT run the expensive experiments.

Do NOT fabricate results.

This harmonisation pass should leave the repository in a clean state ready for the staged MLP/CNN/LSTM modelling prompts.
